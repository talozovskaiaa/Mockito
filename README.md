**Poster Management System**

PosterRepository - это Java-библиотека для управления коллекцией фильмов (постеров). Реализует базовые CRUD-операции (создание, чтение, обновление, удаление) с возможностью сортировки элементов.

_Архитектура_
Основные классы
1. `PosterItem`
   Объект, представляющий фильм с следующими полями:

`id` - уникальный идентификатор фильма

`title` - название фильма

`rating` - рейтинг фильма

`genre` - жанр фильма

2. `PosterRepository`
   Основной класс для управления коллекцией фильмов.

_Конструктор_
`PosterRepository repo = new PosterRepository();`

🛠️ _Требования_
Java 8 или выше

JUnit 5 (для тестирования)

_Запуск тестов:_
`java -jar junit-platform-console-standalone.jar --class-path out --scan-class-path`



